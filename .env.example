# Copy to .env and fill in (optional). .env is gitignored.
OPENAI_API_KEY=
PORT=5000
HOST=127.0.0.1
# Set to 0 to disable Flask debug reloader
FLASK_DEBUG=1
# Optional locally; auto-provisioned on Vercel when a Blob store is linked
BLOB_READ_WRITE_TOKEN=
